export const PROJECT_ROOT = '~/Projects/ethang-one/packages/computer-server';
export const SAVED_FILES_ROOT = `${PROJECT_ROOT}/src/app/commands/saved-files`;

export enum Shell {
  pwsh = 'powershell.exe',
}
