{
  "root": "apps/computer-cli",
  "sourceRoot": "apps/computer-cli/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/computer-cli",
        "main": "apps/computer-cli/src/main.ts",
        "tsConfig": "apps/computer-cli/tsconfig.app.json",
        "assets": ["apps/computer-cli/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/computer-cli/src/environments/environment.ts",
              "with": "apps/computer-cli/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "computer-cli:build"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/computer-cli/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/computer-cli"],
      "options": {
        "jestConfig": "apps/computer-cli/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
