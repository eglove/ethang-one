import { BuildRunProjectsLayout } from '../components/build-run-projects/build-run-projects-layout';

const BuildRunProjects = (): JSX.Element => {
  return <BuildRunProjectsLayout />;
};

export default BuildRunProjects;
